import  {createElement as cElement} from 'react';


function Header() {
  const elem1 = cElement(`div`, {}, `DIVIVIDK`)
  return (
  
        <h1> HEADER  {elem1} </h1> 
  
    );
}

export default Header;
